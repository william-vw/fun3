√ tabling
use python's decorator pattern
intercept calls to the first rule function
call rule function with custom ctu function
    custom ctu function adds call's arguments (= single result) to array
after rule function call ends; all results have been intercepted
save results in memory
for each result, call the original ctu function

- clause re-ordering
simple re-ordering based on dependencies
if output (b/o flow pattern) is ref'ed by other TPs, then put builtin clause directly above first TP
(put builtin clauses right above where they are needed (avoid useless work))
use current mechanism to generate calls; assigns results to re-usable variables

- move away from pure ctu passing
inline rule code into single function
use generators to loop over results as they are generated
first, try some manual performance experiments; impact for zika benchmark?

- more builtins

- optimize for patterns
. e.g., iteration using multiple rules

. see bullets in
/Users/wvw/git/n3/fun3/python/tests-bench/zika/results/code/code_out_red0_it.py